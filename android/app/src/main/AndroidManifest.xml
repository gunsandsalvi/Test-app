<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <application
      android:label="Engine Bench"
      android:icon="@mipmap/ic_launcher">
    <!-- Trusted Web Activity: the bench runs in Chrome's engine (WebView has no
         SharedArrayBuffer, so the worker path is impossible there). Chrome hides its toolbar
         once the origin's /.well-known/assetlinks.json vouches for this app's signing key;
         until then a slim custom-tab header shows, and everything still works. -->
    <meta-data
        android:name="asset_statements"
        android:resource="@string/asset_statements" />
    <activity
        android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
        android:exported="true">
      <meta-data
          android:name="android.support.customtabs.trusted.DEFAULT_URL"
          android:value="https://gunsandsalvi.github.io/Test-app/" />
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="gunsandsalvi.github.io" />
      </intent-filter>
    </activity>
  </application>
</manifest>
